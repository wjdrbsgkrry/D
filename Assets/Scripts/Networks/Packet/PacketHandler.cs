using System;
using System.Collections;
using System.Collections.Generic;
using System.ComponentModel;
using System.Linq;
using System.Text;
using System.Threading.Tasks;
using DummyClient;
using ServerCore;

namespace manager
{
    public class PacketHandler
    {
        
        public static void C_LoginHandler(PacketSession session, IPacket packet)
        {
            
        }
        



        /*
        public static void S_AddFriendHandler(PacketSession session, IPacket packet)
        {
            S_AddFriend p = packet as S_AddFriend;
            Console.WriteLine("AddFriend Packet");
        }

        public static void S_InviteHandler(PacketSession session, IPacket packet)
        {
            S_Invite p = packet as S_Invite;
            Console.WriteLine("Invite Packet");
        }

        public static void S_DeleteFriendHandler(PacketSession session, IPacket packet)
        {
            S_DeleteFriend p = packet as S_DeleteFriend;
            Console.WriteLine("DeleteFriend Packet");
        }

        public static void S_EnterHandler(PacketSession session, IPacket packet)
        {
            S_Enter p = packet as S_Enter;
            Console.WriteLine("Enter Packet");
        }

        public static void S_OutHandler(PacketSession session, IPacket packet)
        {
            S_Out p = packet as S_Out;
            Console.WriteLine("Out Packet");
        }

        public static void S_LoginHandler(PacketSession session, IPacket packet)
        {
            S_Login p = packet as S_Login;
            Console.WriteLine("Login Packet");
        }

        public static void S_LogOutHandler(PacketSession session, IPacket packet)
        {
            S_LogOut p = packet as S_LogOut;
            Console.WriteLine("LogOut Packet");
        }

        public static void S_CheckFriendHandler(PacketSession session, IPacket packet)
        {
            S_CheckFriend p = packet as S_CheckFriend;
            Console.WriteLine("CheckFriend Packet");
        }

        public static void S_AddRoomHandler(PacketSession session, IPacket packet)
        {
            S_AddRoom p = packet as S_AddRoom;
            Console.WriteLine("AddRoom Packet");
        }

        public static void S_DeleteRoomHandler(PacketSession session, IPacket packet)
        {
            S_DeleteRoom p = packet as S_DeleteRoom;
            Console.WriteLine("DeleteRoom Packet");
        }

        public static void S_AttackHandler(PacketSession session, IPacket packet)
        {
            S_Attack p = packet as S_Attack;
            Console.WriteLine("Attack Packet");
        }

        public static void S_AttackSuccessHandler(PacketSession session, IPacket packet)
        {
            S_AttackSuccess p = packet as S_AttackSuccess;
            Console.WriteLine("AttackSuccess Packet");
        }

        public static void S_AttackFailHandler(PacketSession session, IPacket packet)
        {
            S_AttackFail p = packet as S_AttackFail;
            Console.WriteLine("AttackFail Packet");
        }

        public static void S_TurnPassHandler(PacketSession session, IPacket packet)
        {
            S_TurnPass p = packet as S_TurnPass;
            Console.WriteLine("TurnPass Packet");
        }

        public static void S_ReOrderHandler(PacketSession session, IPacket packet)
        {
            S_ReOrder p = packet as S_ReOrder;
            Console.WriteLine("ReOrder Packet");
        }

        public static void S_StartHandler(PacketSession session, IPacket packet)
        {
            S_Start p = packet as S_Start;
            Console.WriteLine("Start Packet");
        }

        public static void S_EndHandler(PacketSession session, IPacket packet)
        {
            S_End p = packet as S_End;
            Console.WriteLine("End Packet");
        }*/
    }
}
